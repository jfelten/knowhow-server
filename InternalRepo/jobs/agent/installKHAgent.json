{
  "id": "install KH Agent",
  "working_dir": "/tmp/KHAgent",
  "options": {
    "timeoutms": 10000
  },
  "files": [],
  "shell": {
    "command": "ssh",
    "args": [
      "-o",
      "StrictHostKeyChecking=no",
      "${USER}@${HOST}"
    ],
    "onConnect": {
      "timeoutms": 30000,
      "responses": {
        "[Pp]assword": "${PASSWORD}"
      },
      "waitForPrompt": "[#$]",
      "errorConditions": [
        "timed out",
        "denied"
      ]
    },
    "onExit": {
      "command": "exit\r\n"
    }
  },
  "script": {
    "env": {
      "USER": "root",
      "PASSWORD": "z3nz1c.c0m",
      "HOST": "10.10.1.1",
      "AGENT_ID": "KHAgent",
      "AGENT_DIR": "/tmp/${AGENT_ID}"
    },
    "commands": [
      {
        "command": "mkdir -p ${AGENT_DIR}"
      },
      {
        "command": "cd ${AGENT_DIR}"
      },
      {
        "command": "type startKHAgent >/dev/null 2>&1 || echo \"KHAgent is not installed\"; GLOBAL_INSTALL=\"false\""
      },
      {
        "command": "if [ -d \"knowhow-agent\" ]; then LOCAL_INSTALL=\"true\"; fi"
      },
      {
        "command": "if [ [ \"${GLOBAL_INSTALL}\" -eq \"false\" ] && [ -n \"${LOCAL_INSTALL}\" ] ]; then npm install knowhow-agent; fi"
      }
    ]
  }
}